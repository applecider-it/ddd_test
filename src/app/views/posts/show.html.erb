<p style="color: green"><%= notice %></p>

<%= render "post", post: @post %>

<div>
  <%= link_to "Edit this post", edit_post_path(@post.id) %> |
  <%= link_to "Back to posts", posts_path %>

  <%= button_to "Destroy this post", post_path(@post.id), method: :delete %>
</div>
